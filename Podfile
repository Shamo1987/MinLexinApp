# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'MinLexinApp' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for MinLexinApp
  pod 'Firebase/Core'
  pod 'Firebase/Database'

  target 'MinLexinAppTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'MinLexinAppUITests' do
    # Pods for testing
  end

end
